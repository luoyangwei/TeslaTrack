# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /api/v1/authorize:
        post:
            tags:
                - Authorize
            description: |-
                CreateAuthorize creates a new OAuth 2.0 client configuration.
                 This is typically an administrative operation to register a new client.
            operationId: Authorize_CreateAuthorize
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.teslatrack.v1.CreateAuthorizeRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.teslatrack.v1.CreateAuthorizeReply'
    /api/v1/authorize/callback:
        get:
            tags:
                - Authorize
            description: |-
                Callback is the endpoint that the OAuth provider calls after user authorization.
                 It receives the authorization code needed to exchange for an access token.
                 see: internal/server/mux.go
            operationId: Authorize_Callback
            parameters:
                - name: code
                  in: query
                  description: The authorization code returned by the OAuth provider.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.teslatrack.v1.CallbackReply'
    /api/v1/authorize/redirect:
        post:
            tags:
                - Authorize
            description: Redirect generates the authorization URL and redirects the user to the OAuth provider.
            operationId: Authorize_Redirect
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.teslatrack.v1.RedirectRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.teslatrack.v1.RedirectReply'
    /api/v1/signin/identifier:
        post:
            tags:
                - Signin
            description: |-
                Identifier provides a method to sign in with account and password.
                 Maps to HTTP POST /api/v1/signin/identifier
            operationId: Signin_Identifier
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.teslatrack.v1.IdentifierRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.teslatrack.v1.IdentifierReply'
    /api/v1/signup/create:
        post:
            tags:
                - Signup
            description: |-
                Creates a new signup request.
                 Maps to HTTP POST /api/v1/signup/create
            operationId: Signup_CreateSignup
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.teslatrack.v1.CreateSignupRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.teslatrack.v1.CreateSignupReply'
    /api/v1/signup/verify:
        get:
            tags:
                - Signup
            description: |-
                Verifies if a signup account already exists.
                 Maps to HTTP GET /api/v1/signup/verify
            operationId: Signup_VerifySignup
            parameters:
                - name: account
                  in: query
                  description: The user's account to verify.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.teslatrack.v1.VerifySignupReply'
    /helloworld/{name}:
        get:
            tags:
                - Greeter
            description: Sends a greeting
            operationId: Greeter_SayHello
            parameters:
                - name: name
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/helloworld.v1.HelloReply'
components:
    schemas:
        api.teslatrack.v1.CallbackReply:
            type: object
            properties: {}
            description: The reply message for the authorization callback. Currently empty.
        api.teslatrack.v1.CreateAuthorizeReply:
            type: object
            properties: {}
            description: The reply message for CreateAuthorize. Currently empty.
        api.teslatrack.v1.CreateAuthorizeRequest:
            type: object
            properties:
                clientId:
                    type: string
                    description: The client identifier. Must be unique.
                clientSecret:
                    type: string
                    description: The client secret. A confidential value.
                grantType:
                    type: string
                    description: The grant type the client is allowed to use (e.g., "authorization_code").
                redirectURI:
                    type: string
                    description: The URI to redirect to after authorization.
            description: The request message for creating a new authorization client.
        api.teslatrack.v1.CreateSignupReply:
            type: object
            properties: {}
            description: The response message for creating a new signup.
        api.teslatrack.v1.CreateSignupRequest:
            type: object
            properties:
                account:
                    type: string
                    description: The user's account.
                password:
                    type: string
                    description: The user's password.
                askedCode:
                    type: string
                    description: The invitation code.
            description: The request message for creating a new signup.
        api.teslatrack.v1.IdentifierReply:
            type: object
            properties:
                accessToken:
                    type: string
                    description: The access token for the session.
                refreshToken:
                    type: string
                    description: The refresh token to get a new access token.
                expireAt:
                    type: string
                    description: The expiration time of the access token.
            description: The response message containing the access token and other details.
        api.teslatrack.v1.IdentifierRequest:
            type: object
            properties:
                account:
                    type: string
                    description: The user's account.
                password:
                    type: string
                    description: The user's password.
            description: The request message containing the user's credentials.
        api.teslatrack.v1.RedirectReply:
            type: object
            properties:
                scope:
                    type: string
                    description: The requested scope of permissions.
                state:
                    type: string
                    description: A random string generated to prevent CSRF attacks.
                nonce:
                    type: string
                    description: A random string generated to prevent replay attacks.
                promptMissingScopes:
                    type: boolean
                    description: Whether to prompt the user for missing scopes.
                requireRequestedScopes:
                    type: boolean
                    description: Whether to require all requested scopes to be granted.
                redirectUri:
                    type: string
                    description: The URI to which the provider will redirect the user.
            description: The reply message containing parameters for the authorization redirect URL.
        api.teslatrack.v1.RedirectRequest:
            type: object
            properties:
                clientId:
                    type: string
                    description: The client ID for which to initiate the authorization flow.
            description: The request message for initiating an authorization redirect.
        api.teslatrack.v1.VerifySignupReply:
            type: object
            properties:
                isRepeated:
                    type: boolean
                    description: Indicates if the account is already registered.
            description: The response message for verifying a signup.
        helloworld.v1.HelloReply:
            type: object
            properties:
                message:
                    type: string
            description: The response message containing the greetings
tags:
    - name: Authorize
      description: The Authorize service provides methods for managing the OAuth 2.0 authorization flow.
    - name: Greeter
      description: The greeting service definition.
    - name: Signin
      description: The Signin service definition.
    - name: Signup
      description: The Signup service definition.
